<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:ev="http://www.w3.org/2001/xml-events"
      xmlns:w2="http://www.inswave.com/websquare"
      xmlns:xf="http://www.w3.org/2002/xforms">
<head>
    <w2:type>COMPONENT</w2:type>

    <xf:model>
        <w2:dataCollection baseNode="map">
            <w2:dataList baseNode="list" repeatNode="map" id="fileDeleteGDS" saveRemovedData="true">
                <w2:columnInfo>
                    <w2:column id="mgntNo" name="관리번호" dataType="text"></w2:column>
                    <w2:column id="wrkSeq" name="작업순번" dataType="text"></w2:column>
                    <w2:column id="fileSeq" name="파일순번" dataType="text"></w2:column>
                </w2:columnInfo>
            </w2:dataList>

            <w2:dataList baseNode="list" repeatNode="map" id="fileUploadGDS" saveRemovedData="true">
                <w2:columnInfo>
                </w2:columnInfo>
            </w2:dataList>
        </w2:dataCollection>

        <w2:workflowCollection></w2:workflowCollection>

        <xf:submission id="sbm_create"
            action=""
            method="post"
            mediatype="application/json"
            mode="asynchronous"
            encoding="UTF-8"
            ref='data:json,[{"id":"fileUploadGDS","key":"IN_DS1"}]'
            target='data:json,[]'
            ev:submitdone="scwin.sbm_create_submitdone"
            ev:submiterror="scwin.sbm_create_submiterror">
        </xf:submission>

        <xf:submission id="sbm_delete"
            action="/cm.bc.vehclinsurancemgnt.cmd.DeleteAttachFilCMD.do"
            method="post"
            mediatype="application/json"
            mode="asynchronous"
            encoding="UTF-8"
            ref='data:json,[{"id":"fileDeleteGDS","key":"IN_DS1"}]'
            target='data:json,[]'
            ev:submitdone="scwin.sbm_delete_submitdone"
            ev:submiterror="scwin.sbm_delete_submiterror">
        </xf:submission>
    </xf:model>

    <w2:layoutInfo></w2:layoutInfo>
    <w2:publicInfo method=""></w2:publicInfo>

    <script type="text/javascript"><![CDATA[
scwin.mgntNo = "";
scwin.wrkSeq = "";

scwin.onpageload = function () {
    scwin.params = $c.data.getParameter();
    if (scwin.params != null) {
        scwin.mgntNo = scwin.params["mgntNo"] || "";
        scwin.wrkSeq = scwin.params["wrkSeq"] || "";
    }
    lc_fileClsCd.setValue("");
};

scwin.onUdcCompleted = function () {
    let codeOptions = [
        { grpCd: "ZZ838", compID: "lc_fileClsCd" }
    ];
    $c.data.setCommonCode(codeOptions);
};

scwin.uploadComplete = function () {
    scwin.f_saveFile();
};

scwin.f_saveFile = async function () {
    sbm_create.action =
        "/cm.bc.vehclinsurancemgnt.cmd.CreateAttachFilCMD.do?mgntNo="
        + scwin.mgntNo + "&wrkSeq=" + scwin.wrkSeq + "&fileClsCd=" + lc_fileClsCd.getValue();
    await $c.sbm.execute(sbm_create);
};

scwin.btn_fileUpload_onclick = async function (e) {
    if (lc_fileClsCd.getValue() == "") {
        await $c.win.alert("증빙파일종류를 선택해 주시기 바랍니다");
        return;
    }
    let chk = await $c.win.confirm("파일을 업로드 하시겠습니까?)");
    if (!chk) {
        return;
    }
    // TODO: $c.gus.cfUploadFile(formObj, fileUploadGDS, "uploadComplete") — WebSquare 파일 업로드 UDC 방식으로 대체 필요
    scwin.uploadComplete();
};

scwin.btn_fileDelete_onclick = async function (e) {
    let checkboxFile = document.getElementsByName("checkFile");

    if (checkboxFile == null || checkboxFile == undefined || checkboxFile.length == 0) {
        await $c.win.alert("삭제 할 파일이 없습니다.");
        return;
    }

    if (checkboxFile.length == 1) {
        if (!checkboxFile[0].checked) {
            await $c.win.alert("삭제 할 파일을 선택해야 합니다.");
            return;
        }

        let chk = await $c.win.confirm("선택하신 파일을 정말로 삭제하시겠습니까?");
        if (chk) {
            fileDeleteGDS.removeAll();
            let delVal = checkboxFile[0].value.split("|");
            let rowIdx = fileDeleteGDS.insertRow();
            fileDeleteGDS.setCellData(rowIdx, "mgntNo", delVal[0]);
            fileDeleteGDS.setCellData(rowIdx, "fileSeq", delVal[1]);

            await $c.sbm.execute(sbm_delete);
        }
    } else {
        let checkedCnt = 0;
        for (let i = 0; i < checkboxFile.length; i++) {
            if (checkboxFile[i].checked) {
                checkedCnt += 1;
            }
        }

        if (checkedCnt == 0) {
            await $c.win.alert("선택된 파일이 없습니다.");
            return;
        }

        let chk = await $c.win.confirm("선택하신 파일을 정말로 삭제하시겠습니까?");
        if (chk) {
            let cbCnt = checkboxFile.length;
            fileDeleteGDS.removeAll();
            for (let i = 0; i < cbCnt; i++) {
                if (checkboxFile[i].checked) {
                    let delVal = checkboxFile[i].value.split("|");
                    let rowIdx = fileDeleteGDS.insertRow();
                    fileDeleteGDS.setCellData(rowIdx, "mgntNo", delVal[0]);
                    fileDeleteGDS.setCellData(rowIdx, "fileSeq", delVal[1]);
                }
            }

            await $c.sbm.execute(sbm_delete);
        }
    }
};

scwin.sbm_create_submitdone = async function (e) {
    await $c.win.alert("업로드가 완료 되었습니다.");
    // TODO: document.location.reload() → WebSquare 방식으로 화면 갱신 필요
    // TODO: parent.f_Retrieve() → 부모 화면 조회 호출 방식 확인 필요
};

scwin.sbm_create_submiterror = function (e) {
    $c.gus.cfShowError(e);
};

scwin.sbm_delete_submitdone = async function (e) {
    await $c.win.alert("삭제가 완료 되었습니다.");
    // TODO: document.location.reload() → WebSquare 방식으로 화면 갱신 필요
    // TODO: parent.f_Retrieve() → 부모 화면 조회 호출 방식 확인 필요
};

scwin.sbm_delete_submiterror = function (e) {
    $c.gus.cfShowError(e);
};
]]></script>

    <w2:require as="udc_fileMultiUpload" type="page" variableClone="true" src="/cm/udc/fileMultiUpload.xml"></w2:require>
</head>

<body ev:onpageload="scwin.onpageload">
    	<xf:group class=" row-gap-8" id="" style="">
    		<xf:group class="lybox" id="" style="">
    			<xf:group class="col" id=""><xf:group id="" class="file-wrap no-title"><w2:udc_fileMultiUpload filter="gif, jpg, bmp, png, zip, txt, ppt, pptx, xls, xlsx, doc, docx, xml, csv, pdf" maxFileCount="10" style="" totalFileSize="2009715200" maxFileSize="536870912" fileGrpSeq="">
    					</w2:udc_fileMultiUpload></xf:group></xf:group>
    			<xf:group class="col" id="">
    			<xf:group id="" class="file-wrap no-title"><w2:udc_fileMultiUpload filter="gif, jpg, bmp, png, zip, txt, ppt, pptx, xls, xlsx, doc, docx, xml, csv, pdf" maxFileCount="10" style="" totalFileSize="2009715200" maxFileSize="536870912" fileGrpSeq="">
    					</w2:udc_fileMultiUpload></xf:group></xf:group>
    		</xf:group>
    	</xf:group>
    </body>
</html>