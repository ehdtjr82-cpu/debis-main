<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
    xmlns:ev="http://www.w3.org/2001/xml-events"
    xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
    <head meta_screenName="topGrdBtn" meta_screenId="topGrdBtn" meta_type="">
        <w2:type palette="support">COMPONENT</w2:type>
        <w2:buildDate/>
        <w2:MSA/>
        <xf:model>
            <w2:dataCollection baseNode="map"/>
            <w2:workflowCollection/>
        </xf:model>
        <w2:layoutInfo/>
        <w2:publicInfo method="scwin.setGridId,scwin.setDownOpts,scwin.setDisabledBtn,scwin.setHideBtn,scwin.getUpExt,scwin.uploadGridViewExcel"/>
        <script lazy="false" type="text/javascript"><![CDATA[
/**
 * @component
 * @componentName udc_topGrdBtn
 * @pluginName 
 * @company
 * @developer
 * @category /cm/udc
 * @notSupportBrowser 
 * @version
 * @htmlRender
 * @icon
 * @disableIcon
 * @description 그리드 상단 버튼 모음
 * @width 268
 * @height 22
 * @license
 * @imagePath
 * @homepage
 */

/**
 * @property
 * @name id
 * @category 01.Basic & ETC
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description desc
 */

/**
 * @property
 * @name class
 * @category 01.Basic & ETC
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description desc
 */

/**
 * @property
 * @name style
 * @category 01.Basic & ETC
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description desc
 */

/**
 * @property
 * @name grp
 * @category 01.Basic & ETC
 * @type string
 * @option
 * @default
 * @necessary Y
 * @bindparent
 * @description 그리드 를 포함한 그룹 아이디
 */

/**
 * @property
 * @name grdUpOpts
 * @category 09.Grid Settings
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description 그리드 업로드용 옵션
 */

/**
 * @property
 * @name grdDownOpts
 * @category 09.Grid Settings
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description 그리드 다운로드용 옵션
 */

/**
 * @property
 * @name gridID
 * @category 09.Grid Settings
 * @type string
 * @option
 * @default
 * @necessary Y
 * @bindparent
 * @description 그리드 아이디 (엑셀업, 다운용)
 */

/**
 * @property
 * @name gridUpYn
 * @category 05.Design
 * @type combobox
 * @option [Y,N]
 * @default Y
 * @necessary N
 * @bindparent
 * @description 엑셀 업로드 버튼 사용유무
 */

/**
 * @property
 * @name gridDownYn
 * @category 05.Design
 * @type combobox
 * @option [Y,N]
 * @default Y
 * @necessary N
 * @bindparent
 * @description 엑셀 다운로드 사용유무
 */

/**
 * @property
 * @name templateYn
 * @category 05.Design
 * @type combobox
 * @option [Y,N]
 * @default N
 * @necessary N
 * @bindparent
 * @description 템플릿 다운로드 사용유무
 */

/**
 * @property
 * @name gridUpUserAuth
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description gridUp 버튼 UserAuth
 */

/**
 * @property
 * @name gridDownUserAuth
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description gridDown 버튼 UserAuth
 */

/**
 * @property
 * @name templateUserAuth
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description template 버튼 UserAuth
 */

/**
 * @property
 * @name gridUpObjType
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description gridUp 버튼 ObjType
 */

/**
 * @property
 * @name gridDownObjType
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description gridDown 버튼 ObjType
 */

/**
 * @property
 * @name templateObjType
 * @category 03.Data
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description template 버튼 ObjType
 */

/**
 * @property
 * @name gridDownFn
 * @category 09.Grid Settings
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description 그리드 다운로드 함수
 */

/**
 * @property
 * @name btnPlusYn
 * @category 05.Design
 * @type combobox
 * @option [Y,N]
 * @default Y
 * @necessary N
 * @bindparent
 * @description +- 버튼 노출 여부
 */

/**
 * @property
 * @name btnUser
 * @category 05.Design
 * @type combobox
 * @option [Y,N]
 * @default Y
 * @necessary N
 * @bindparent
 * @description 개인화 버튼 사용유무
 */

/**
 * @property
 * @name gridUpFn
 * @category 09.Grid Settings
 * @type string
 * @option
 * @default
 * @necessary N
 * @bindparent
 * @description 그리드 업로드 function 명
 */

scwin.onpageload = function() {
	scwin.opts = $p.getOptions();
    scwin.grp = $p.parent().$p.getComponentById(scwin.opts.grp);
    scwin.grid = $p.parent().$p.getComponentById(scwin.opts.gridID);
    // scwin.gridDownFn = eval("$p.parent()."+scwin.opts.gridDownFn);

    let parentWin = $p.getParentWindow();
    let pScwin = parentWin["scwin"];
    let gridDownFn;

    if(typeof scwin.opts.gridDownFn !== "undefined") {
        gridDownFn =  scwin.opts.gridDownFn.replace(/scwin./g,"");
        scwin.gridDownFn =  pScwin[gridDownFn];
    }
    if(typeof scwin.opts.gridUpFn !== "undefined") {
        gridUpFn =  scwin.opts.gridUpFn.replace(/scwin./g,"");
        scwin.gridUpFn =  pScwin[gridUpFn];
    }

    if(scwin.opts.gridUpUserAuth) {
        btn_excelUp.options.userAuth = scwin.opts.gridUpUserAuth;
    }
    if(scwin.opts.gridDownUserAuth) {
        btn_excelDown.options.userAuth = scwin.opts.gridDownUserAuth;
    }
    if(scwin.opts.templateUserAuth) {
        btn_templateDw.options.userAuth = scwin.opts.templateUserAuth;
    }
    if(scwin.opts.gridUpObjType) {
        btn_excelUp.options.objType = scwin.opts.gridUpObjType;
    }
    if(scwin.opts.gridDownObjType) {
        btn_excelDown.options.objType = scwin.opts.gridDownObjType;
    }
    if(scwin.opts.templateObjType) {
        btn_templateDw.options.objType = scwin.opts.templateObjType;
    }

    scwin.requestRefreshUserAuth();

    // 엑셀 업로드 사용유무
    if(scwin.opts.gridUpYn == 'N') {
        btn_excelUp.hide();
        lc_ext.hide();
    }
    // 엑셀 다운로드 사용유무
    if(scwin.opts.gridDownYn == 'N') {
        btn_excelDown.hide();
    }
    // 템플릿 다운로드 사용유무
    if(scwin.opts.templateYn == 'Y') {
        btn_templateDw.show("");
    }
    // 확대/축소
    if(scwin.opts.btnPlusYn == 'N') {
        grpBtnPlus.hide();
    } else {
        let vRow = 0;
        if(typeof scwin.grid !== "undefined") {
            scwin.grid.options.visibleRowNumFix = "false";
            vRow = scwin.grid.options.initFixedHeightByRowNum > -1 ? scwin.grid.options.initFixedHeightByRowNum : scwin.grid.options.visibleRowNum;
            scwin.grid.options.visibleRowNum = "";
            scwin.grid.options.initFixedHeightByRowNum = vRow;
            scwin.grid.setVisibleRowNum(vRow);
        } 
        if(vRow > 0) {
            scwin.grid.options.initRowNumCnt = parseInt(scwin.grid.options.visibleRowNum);
        }
    }

    // 개인화
    if(scwin.opts.btnUser == 'N') {
        btn_user.hide();
        btn_userSave.hide();
    } else {
        if(scwin.grid) {
            $c.win.getGridSetting(scwin.grid);
        }
    }

    // scwin.grp = eval("$p.parent()."+scwin.opts.grp);
};

scwin.requestRefreshUserAuth = function () {
    let parentWin = $p.getParentWindow();

    if (!parentWin || !parentWin.scwin || typeof parentWin.scwin.setUserAuth !== "function") {
        return;
    }

    if (parentWin.__topGrdBtnUserAuthTimer) {
        clearTimeout(parentWin.__topGrdBtnUserAuthTimer);
    }

    parentWin.__topGrdBtnUserAuthTimer = setTimeout(function () {
        parentWin.__topGrdBtnUserAuthTimer = null;
        parentWin.scwin.setUserAuth();
    }, 50);
};

// 확장
scwin.btn_trigger1_onclick = function (e) {
    $c.win.extension(scwin.grp);
};

// 축소
scwin.btn_trigger2_onclick = function (e) {
    $c.win.reduction(scwin.grp);
};

// 엑셀다운
scwin.btn_excelDown_onclick = function (e) {
    try {
        if(scwin.gridDownFn) {
            scwin.gridDownFn();
        } else {
            let options = "";
            if(!$c.gus.cfIsNull(scwin.opts.grdDownOpts)) {
                options = JSON.parse( scwin.opts.grdDownOpts) || {};
            }
            $c.data.downloadGridViewExcel(scwin.grid,  options);
        }
    } catch(e){
        $c.win.alert("그리드 다운로드 옵션을 확인해 주시기 바랍니다.");
    }
};

// 엑셀 업로드
scwin.btn_excelUp_onclick = function (e) {
    try {
         if(scwin.gridUpFn) {
            scwin.gridUpFn();
        } else {
            let options = "";
            if(!$c.gus.cfIsNull(scwin.opts.grdUpOpts)) {
                options = JSON.parse( scwin.opts.grdUpOpts) || {};
            }
            if(lc_ext.getValue() == "1") { // csv  일때
                $c.data.uploadGridViewCSV(scwin.grid,  options);
            } else {
                $c.data.uploadGridViewExcel(scwin.grid,  options);
            }
        }
    } catch(e){
        $c.win.alert("그리드 업로드 옵션을 확인해 주시기 바랍니다.");
    }
};

scwin.btn_templateDw_onclick = function (e) {

};

// 개인화
scwin.btn_user_onclick = function (e) {;
    $c.win.gridSetPopUp(scwin.grid, this.id);
};

/**
 * @method
 * @name setGridId
 * @description 그리드 아이디 변경
 * @param {string} gridID 그리드 아이디
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.setGridId = function (gridID) {
    scwin.grid = $p.parent().$p.getComponentById(gridID);
    return scwin.grid;
};

/**
 * @method
 * @name setDownOpts
 * @description 그리드 다운로드 옵션변경
 * @param {json} jsonData desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.setDownOpts = function (jsonData) {
    if(!$c.gus.cfIsNull(jsonData)) {
	    scwin.opts.grdDownOpts = jsonData;
    }
};

/**
 * @method
 * @name setDisabledBtn
 * @description 버튼 활성화/비활성화
 * @param {string} btnId 버튼아이디
 * @param {boolean} tp 활성화 여부
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.setDisabledBtn = function (btnId, tp) {
	let btn = $p.getComponentById(btnId);
    btn.setDisabled(tp);
};

/**
 * @method
 * @name setHideBtn
 * @description 버튼을 show/hide
 * @param {string} btnId desc
 * @param {boolean} tp desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.setHideBtn = function (btnId, tp) {
	let btn = $p.getComponentById(btnId);
    tp == "true" ? btn.hide() : btn.show("");
};

/**
 * @method
 * @name getUpExt
 * @description 업로드 확장자
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.getUpExt = function () {
    return lc_ext.getValue();
};

/**
 * @method
 * @name uploadGridViewExcel
 * @description 업로드
 * @param {object} grid grid 객체
 * @param {object} opt 옵션
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.uploadGridViewExcel = function (grid, opt) {
    opt = typeof opt !== "undefined" ? opt : {};

    if(lc_ext.getValue() == "1") { // csv  일때
        $c.data.uploadGridViewCSV(grid,  opt);
    } else {
        $c.data.uploadGridViewExcel(grid,  opt);
    }
};

// 컬럼 저장
scwin.trigger3_onclick = function (e) {
    $c.win.gridSetting(scwin.grid,undefined,"colMove");
};
]]></script>
    </head>
    <body ev:onpageload="scwin.onpageload">
        <xf:group id="" class="section col-gap-8">
        	<xf:group id="topBtnGrp" class="inner">
                <xf:select1 id="lc_ext" selectedIndex="0" appearance="full" style="" cols="" rows="" ref="" renderType="radiogroup">
                    <xf:choices>
                        <xf:item>
                            <xf:label><![CDATA[xlsx]]></xf:label>
                            <xf:value><![CDATA[0]]></xf:value>
                        </xf:item>
                        <xf:item>
                            <xf:label><![CDATA[csv]]></xf:label>
                            <xf:value><![CDATA[1]]></xf:value>
                        </xf:item>
                    </xf:choices>
                </xf:select1>
                <xf:trigger style="" id="btn_excelUp" type="button" class="btn excel-up"
                    ev:onclick="scwin.btn_excelUp_onclick">
                    <xf:label><![CDATA[엑셀 업로드]]></xf:label>
                </xf:trigger>
        		<xf:trigger style="" id="btn_excelDown" type="button" class="btn excel-down"
        			ev:onclick="scwin.btn_excelDown_onclick">
        			<xf:label><![CDATA[엑셀 다운로드]]></xf:label>
        		</xf:trigger>
        		<xf:trigger class="btn template" id="btn_templateDw" style="display: none;" title="템플릿 다운" type="button"
        			ev:onclick="scwin.btn_templateDw_onclick">
        			<xf:label><![CDATA[템플릿 다운로드]]></xf:label>
        		</xf:trigger>
        		<xf:trigger style="" id="btn_user" type="button" class="btn btn_grid_set_item_sel"
        			ev:onclick="scwin.btn_user_onclick">
        			<xf:label><![CDATA[개인화]]></xf:label>
        		</xf:trigger>
        		<xf:trigger class="btn" id="btn_userSave" style="" type="button" ev:onclick="scwin.trigger3_onclick">
        			<xf:label><![CDATA[컬럼저장]]></xf:label>
        		</xf:trigger>
        	</xf:group>

        	<xf:group id="grpBtnPlus" class="row-box">
        		<xf:trigger ev:onclick="scwin.btn_trigger1_onclick" style="" id="btn_trigger1" type="button"
        			class="btn ico row-add">
        			<xf:label><![CDATA[+]]></xf:label>
        		</xf:trigger>
        		<xf:trigger ev:onclick="scwin.btn_trigger2_onclick" style="" id="btn_trigger2" type="button"
        			class="btn ico row-del">
        			<xf:label><![CDATA[-]]></xf:label>
        		</xf:trigger>
        		
        	</xf:group>
        </xf:group>
    </body>
</html>
